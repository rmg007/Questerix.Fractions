# Nightly Report — 2026-05-08

## Summary
- 🔴 Critical: 0
- 🟡 Warning: 6
- 🟢 Info: 18
- 🆕 New since 2026-05-07-fix-pr: 5
- ♻️ Carried over from 2026-05-07-fix-pr: 2

PR #102 (Settings a11y restoration) merged as `3af21af` — all 6 actionable findings from the prior cycle are now resolved on `main`. Tonight's swarm reports zero critical issues. New surface area surfaced four sibling A11yLayer/reduced-motion gaps that the Settings fix did not cover (overlay, banner, version-tap, vignette helper).

## 🔴 Critical Findings
*None.*

## 🟡 Warnings
**[W-1]** Version label has sub-44px touch target and no A11yLayer parity (researcher unlock triple-tap) — *Source: a11y-auditor → `src/scenes/settings/versionTapToggle.ts:50`*

**[W-2]** `QuestCompleteOverlay` interactive buttons have no A11yLayer DOM mirror — *Source: a11y-auditor → `src/components/QuestCompleteOverlay.ts:173, 201`* — sibling pattern to the Settings fix in #102.

**[W-3]** `UpdateBanner` uses `TestHooks.mountInteractive` (E2E-only, gated behind `?testHooks=1`) instead of `A11yLayer.mountAction` — production screen-reader users miss the "tap to update" prompt — *Source: a11y-auditor → `src/components/UpdateBanner.ts:80`*

**[W-4]** `VignetteAnimations.ts` calls `scene.tweens.add()` 30+ times with no `checkReduceMotion()` guard — bypasses the project-wide `tween()` helper that auto-respects `prefers-reduced-motion` — *Source: a11y-auditor → `src/components/VignetteAnimations.ts:53`*

**[W-5]** `phaser` chunk at 90.0% of its 380 KB sub-budget (342.1 KB gzipped) — zero headroom for any phaser bump — *Source: bundle-watcher → `dist/assets/phaser-DM3nDuma.js`* — carry-forward from 2026-05-07.

**[W-6]** Application manifest fails JSON parse at boot (`The manifest is not valid JSON data.`) — *Source: `test-results/nightly-screenshots/console.log:1`* — carry-forward from 2026-05-07.

## 🟢 Info / Observations
**[I-1]** Total gzipped JS: 518.3 KB / 1.0 MB budget (50.6%). Brotli total: 422.9 KB. — *bundle-watcher BUN-1*

**[I-2]** Bundle delta vs 2026-05-07: +484 bytes (+0.09%) — entirely within noise floor; attributable to `3af21af` Settings refactor. — *bundle-watcher BUN-4*

**[I-3]** Per-chunk health: `scenes` 82.1 KB / 100 KB (+0.4 KB drift, 82.1%); `dexie` 30.3 KB / 35 KB (86.4%, unchanged); `observability` 8.2 KB / 12 KB (68.1%, dormant in default builds but still shipped). — *bundle-watcher BUN-3, BUN-5*

**[I-4]** Curriculum byte-parity holds across all three surfaces: `public/curriculum/v1.json`, `src/curriculum/bundle.json`, and the deployed copy fetched at runtime all sha256 to `e100939946cad525edf755b832c0404c49846f6590df19c0ff0cd2cbc64f3541` (26,030 bytes). — *curriculum-byte-parity CUR-1*

**[I-5]** C1–C10 fully clean across 4 source-touching commits (`9dfdff1..31ba38e`). No new egress, no new deps, no new localStorage writes, no curriculum/level changes, no neon/particle additions. Settings refactor explicitly **restored** C7 compliance (touch targets back to 100 canvas px ≈ ≥44 CSS px @ 360 vw). — *c1-c10-auditor C-1..C-10*

**[I-6]** Settings refactor fix (#102 / `3af21af`) verified clean: 7 action buttons all registered via `A11yLayer.mountAction`, reset confirm modal pushes/pops a layer with focus-to-Cancel default, `cameras.fadeIn` guarded by `matchMedia('(prefers-reduced-motion: reduce)')`. No regressions. — *a11y-auditor A11Y-5*

**[I-7]** Engine determinism: zero findings — no host-global RNG/clock calls introduced under `src/engine/**`. — *engine-determinism-auditor*

**[I-8]** localStorage at boot is empty (`{}`) on a fresh nightly profile — no orphaned student keys, no unknown keys, matches expected post-onboarding flow. — *`localstorage.json`*

**[I-9]** Canvas sizing on iPhone-class viewport (390×844): canvas 800×1280 → CSS 390×624, no right/bottom clip. Portrait 800×1280 backing store is correct. — *`canvas-sizing.json`*

**[I-10]** CSP working as intended: Cloudflare beacon (`static.cloudflareinsights.com/beacon.min.js`) refused — preserves C1 no-egress posture. — *`console.log:2-3`*

**[I-11]** 4 source-touching commits since prior nightly (plus 4 doc/report chores): `31ba38e` (docs), `a6e6f95` (nightly swarm infra, +1070 LOC across `.github/workflows/`, `scripts/`, `tests/e2e/`), `3af21af` (Settings a11y fix, ±240/-155 LOC), report archive chore.

## 🆕 New Since 2026-05-07-fix-pr
- **W-1** (versionTapToggle a11y gap)
- **W-2** (QuestCompleteOverlay A11yLayer gap)
- **W-3** (UpdateBanner A11yLayer gap)
- **W-4** (VignetteAnimations reduced-motion bypass)
- **I-6** (Settings #102 verification)
- **I-7** (engine determinism clean — first time captured this cycle)

## ♻️ Unchanged from 2026-05-07-fix-pr
- **W-5** (phaser chunk at 90% of sub-budget) — was W-4 in prior report
- **W-6** (manifest JSON parse error) — was W-5 in prior report

The six Settings-scope findings (C-1, C-2, C-3, W-1, W-2, W-3 of `2026-05-07.md`) are now **resolved** by merged commit `3af21af` and verified clean by tonight's a11y audit (A11Y-5).

## Swarm Audit Summary
| Subagent | Findings |
|----------|----------|
| a11y-auditor | 5 findings (4 warning, 1 info) |
| bundle-watcher | 5 findings (1 warning, 4 info) |
| c1-c10-auditor | 10 findings (10 info — all C1–C10 clean) |
| curriculum-byte-parity | 1 finding (info — sha256 match) |
| engine-determinism-auditor | 0 findings |

## Commits Since Last Report
- `31ba38e` docs(claude): replace GitHub MCP with gh CLI for all PR operations
- `a6e6f95` chore: nightly test/fix swarm infrastructure + GitHub auto-merge
- `3af21af` fix(settings): a11y regressions from Settings refactor — touch targets + A11yLayer parity (#102)
- `9dfdff1` chore: archive 2026-05-07 nightly report after fixes shipped
- `1c69dca` chore: nightly report 2026-05-07
- `c8ddfa8` chore: nightly report 2026-05-07
- `fd20e2d` chore: nightly report 2026-05-07

## Screenshots Captured
- `test-results/nightly-screenshots/menu-chromium.png` — Chromium MenuScene render
- `test-results/nightly-screenshots/menu-webkit.png` — WebKit MenuScene render
- `test-results/nightly-screenshots/console.log` — boot console output
- `test-results/nightly-screenshots/localstorage.json` — fresh-profile LS audit
- `test-results/nightly-screenshots/canvas-sizing.json` — viewport/canvas geometry
- `test-results/nightly-screenshots/curriculum-hash.json` — deployed vs bundle sha256

## Unstaged Source Changes at Test Time
None in `src/`. Working tree carries doc-only modifications (sync-claude-md edits to `.claude/agents/*.md`, `.claude/commands/*.md`, `CLAUDE.md`) and accumulated `.claude/sessions/*.md` session logs — no impact on nightly findings.
