# Nightly Report — 2026-05-07

## Summary
- 🔴 Critical: 2
- 🟡 Warning: 4
- 🟢 Info: 6
- 🆕 New since (none): 12

## 🔴 Critical Findings

**[C-1]** First-run E2E flow fails on all 3 browsers (chromium-desktop, mobile-portrait, webkit-mobile) — *Source: Playwright e2e — `tests/e2e/first-run.spec.ts:33,50,74` × 3 browsers = 9 failures* — *Why critical: FirstRunScene gates onboarding/MVP gate #1; complete cross-browser failure indicates regression in boot→FirstRunScene routing or sentinel detection (`first-run-scene` testid).*

**[C-2]** WebKit-mobile boot is broken — `boot-start-btn` never becomes visible — *Source: `tests/e2e/responsive-audit.spec.ts:42`, `tests/e2e/settings.spec.ts:53`, `tests/e2e/smoke.spec.ts` (webkit-mobile)* — *Why critical: C7 mandates iOS Safari support; if `boot-start-btn` cannot render in WebKit, all downstream user flows are blocked on iOS Safari. Production health check passes (HTML loads), so failure is JS/canvas-side.*

## 🟡 Warnings

**[W-1]** 5 flaky tests across browsers — *Source: Playwright e2e (smoke, settings, responsive-audit)* — Flake suggests boot timing variance >2s; cache state, SW lifecycle, or font-load races likely culprits.

**[W-2]** Unstaged source changes in `src/scenes/SettingsScene.ts` and helpers (3 files) repositioning buttons, adopting adventure background, and shifting status text from y=1180 → y=1095 — *Source: `git status` + diff* — Status text at y=1095 (font 26px, spans ~1080–1110) sits ~5 CSS px inside the Back button hit area (y=1145, h=80, top ≈ 1105). Minor visual bleed; status auto-clears after 3s in nominal path.

**[W-3]** `npm run lint`/`typecheck`/`test:unit` were not run on the unstaged Settings refactor before this report — *Source: nightly inputs* — Pre-push gates not exercised; if a commit lands tonight, hooks will catch it but no early signal.

**[W-4]** 41 untracked session files in `.claude/sessions/` plus `.eslintcache (1)` and 3 new `scripts/nightly-*` files — *Source: `git status`* — Working tree is noisy; consider `.gitignore` entries for `.eslintcache (1)` (space-suffixed duplicate) and pruning old session logs.

## 🟢 Info / Observations

**[I-1]** Production health check 18/18 passed — root, sw.js, registerSW.js, manifest, curriculum/v1.json, SPA fallback, all 5 security headers, A11yLayer shipped, no third-party analytics, no restrictive COEP. Build pinned to `0d96706` / `2026-05-07T12:59:38.305Z`.

**[I-2]** CSP, HSTS (max-age=63072000), X-Frame-Options DENY, Referrer-Policy no-referrer, Permissions-Policy disabling sensors all present in HTTP headers — defense-in-depth posture intact.

**[I-3]** 28 e2e tests still passing on chromium-desktop and mobile-portrait — core boot→menu→L1→FeedbackOverlay flow remains green outside webkit.

**[I-4]** SettingsScene.ts diff reads as a deliberate UX redesign (adventure background, title font upgrade, paired Export/Restore + Update/Refresh buttons, full-width Reset). No introduced bugs in `ResetDeviceHandler.ts` or `versionTapToggle.ts` on read.

**[I-5]** No new unrecognized localStorage keys observed beyond the known baseline (`lastUsedStudentId`, `unlockedLevels:*`, `completedLevels:*`). Postdeploy scrape did not surface multi-student orphan keys; cannot count distinct student IDs without live browser scrape.

**[I-6]** Curriculum endpoint `/curriculum/v1.json` returns 200 and reports `version:1` — bundle still wired and fetchable from CDN.

## 🆕 New Since (none)
C-1, C-2, W-1, W-2, W-3, W-4, I-1, I-2, I-3, I-4, I-5, I-6 — first nightly report; all findings are new.

## ♻️ Unchanged from (none)
None — no prior report to carry forward.

## Unstaged Source Changes at Test Time
- `src/scenes/SettingsScene.ts` — UX redesign: adventure background, title typography, side-by-side button pairs, status text repositioned y=1180→1095
- `src/scenes/settings/ResetDeviceHandler.ts` — read-clean; no obvious bugs
- `src/scenes/settings/versionTapToggle.ts` — read-clean; triple-tap researcher unlock-gate bypass intact
