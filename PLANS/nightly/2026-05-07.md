# Nightly Report — 2026-05-07

## Summary
- 🔴 Critical: 1
- 🟡 Warning: 4
- 🟢 Info: 7
- 🆕 New since (none): 12

## 🔴 Critical Findings

**[C-1]** Webkit-mobile (iOS Safari proxy) first-run flow is consistently broken — all 3 first-run.spec.ts tests fail on every retry (`fresh device boots to FirstRunScene`, `skip button … creates Player 1 …`, `returning user with valid lastUsedStudentId skips FirstRunScene`) — *Source: Playwright e2e — `tests/e2e/first-run.spec.ts:33,50,74` on webkit-mobile project; 3 of 13 failures are non-flaky and all on webkit-mobile* — *Why critical: C7 mandates iOS Safari support. The matching tests pass on retry under chromium-desktop and mobile-portrait, so the problem is webkit-specific (likely IndexedDB/SW/timing). FirstRunScene is MVP gate #1; iOS users cannot complete onboarding.*

## 🟡 Warnings

**[W-1]** 10 flaky e2e tests in this run — first-run × 3 browsers (chromium-desktop, mobile-portrait, webkit-mobile only on `:33`/`:50`/`:74` mixed), `nightly-browser-check.spec.ts:36` × 3 browsers, `settings.spec.ts:29` on webkit-mobile. All passed on retry #1 — *Source: Playwright "Flaky" list* — Suggests boot-time variance >2 s under cold cache; SW + IndexedDB + font load races are the usual culprits. Worth tightening `boot-start-btn` visibility wait or adding a deterministic readiness signal.

**[W-2]** webkit-mobile `settings.spec.ts:29` showed `boot-start-btn` not visible at 8000ms timeout in the captured error block, even though the run-summary categorizes it as flaky (passed on retry). On iOS Safari this test sits adjacent to the C-1 cluster — a single boot stall could explain both. Worth treating as the same root cause until proven otherwise — *Source: error block at top of Playwright output + flaky list*

**[W-3]** Unstaged source changes in `src/scenes/SettingsScene.ts` (full UX redesign: adventure background, side-by-side Export/Restore + Update/Refresh pairs, full-width Reset, status text moved to y=1095) plus `ResetDeviceHandler.ts` and `versionTapToggle.ts` carried over from prior session — *Source: `git status` + read of all 3 files* — Status text at y=1095 (font 26px ≈ height 30) overlaps the top of the Back button at y=1145 (h=80, top ≈ 1105). Visual bleed for the ~3s the status persists. Files read clean otherwise — no introduced bugs spotted in `ResetDeviceHandler` or `versionTapToggle`.

**[W-4]** Working tree noise: 37+ untracked `.claude/sessions/2026-05-0[67]-*.md` files, `.eslintcache (1)` (space-suffixed duplicate), `playwright.nightly.config.ts`, 3 `scripts/nightly-*` files — *Source: `git status`* — These should either be `.gitignore`d or committed. The space-suffixed `.eslintcache (1)` is almost certainly a Windows duplicate-on-write artifact and should be deleted.

## 🟢 Info / Observations

**[I-1]** Production health check 18/18 passed — root, sw.js, registerSW.js, manifest, curriculum/v1.json, SPA fallback, all 5 security headers, A11yLayer shipped, no third-party analytics, no restrictive COEP. Build is `fd20e2d` / `2026-05-08T05:14:11.360Z`.

**[I-2]** Curriculum hash matches: deployed `e100939…f3541` ≡ bundle.json `e100939…f3541`. No drift between repo and CDN.

**[I-3]** Bundle size 517.9 KB gzipped (50.6% of 1 MB budget) — well under budget. Per-chunk: phaser 342.1 KB / 380 KB (90%), scenes 81.7 KB / 100 KB, observability 8.2 KB / 12 KB, dexie 30.3 KB / 35 KB. All chunks PASS.

**[I-4]** Security headers green: HSTS max-age=63072000, X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy no-referrer, Permissions-Policy with sensors disabled, CSP `default-src 'self'` with strict script-src.

**[I-5]** Canvas dimensions 800×1280 (portrait) with cssW=390 / cssH=624, viewport 390×844, no clipping right or bottom — correct sizing per design.

**[I-6]** Browser-side localStorage scrape returned empty (`lsData: {}`, no studentIds, no orphans) — fresh-context test setup; no surprise persistence keys leaked. Curriculum sha256 match confirmed in-browser.

**[I-7]** 50 e2e tests passed (out of 63 attempted with retries). Test pass rate by category: postdeploy 18/18, bundle/security all green, 50 of 63 Playwright tests green after retries. Net effective failure: 3 (all webkit-mobile first-run).

## 🆕 New Since (none)
C-1, W-1, W-2, W-3, W-4, I-1, I-2, I-3, I-4, I-5, I-6, I-7 — treated as fresh nightly per "Previous report: (none)" header (prior 2026-05-07 report on disk was for build `0d96706`; this report covers the newer `fd20e2d` deploy).

## ♻️ Unchanged from (none)
None.

## Commits Since Last Report
- `fd20e2d` chore: nightly report 2026-05-07
- `0d96706` chore: remove branch-name enforcement from pre-push hook (#101)
- `3142b7d` plans: mark all 4 phases complete — PRs #98 #99 #100 merged
- `d5a395f` fix(boot): suppress storage-error spam and prevent mid-session SW reload (#100)
- `6a97769` fix: add 600ms safety timeout to fadeAndStart + guard boot scenes from tween-pause (BUG-MOB-01) (#99)

## Screenshots Captured
- `test-results/e2e-settings-Settings-scen-2258c-ers-settings-scene-sentinel-webkit-mobile/test-failed-1.png`
- `test-results/e2e-settings-Settings-scen-2258c-ers-settings-scene-sentinel-webkit-mobile-retry1/test-failed-1.png`
- (Additional Playwright failure traces and screenshots under `test-results/` for the 13 initial failures — webkit-mobile first-run × 3 retained as consistent failures)

## Unstaged Source Changes at Test Time
- `src/scenes/SettingsScene.ts` — full UX redesign (adventure background, paired buttons, status text y=1180→1095)
- `src/scenes/settings/ResetDeviceHandler.ts` — read-clean; matches existing pattern; uses AccessibilityAnnouncer + db.delete + lastUsedStudent.clear
- `src/scenes/settings/versionTapToggle.ts` — read-clean; triple-tap researcher unlock-gate bypass intact (`toggleUnlockGateBypass` from preferences)
