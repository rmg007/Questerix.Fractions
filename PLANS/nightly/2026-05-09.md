# Nightly Report — 2026-05-09

## Summary
- 🔴 Critical: 4
- 🟡 Warning: 7
- 🟢 Info: 13
- 🆕 New since 2026-05-08: 11
- ♻️ Carried over from 2026-05-08: 2

All four actionable warnings from 2026-05-08 (W-1 versionTapToggle, W-2 QuestCompleteOverlay, W-3 UpdateBanner, W-4 VignetteAnimations) were resolved this cycle by groups G1–G4 (`c2966df`, `38a73e4`, `b94071c`, `4edb7c2`). Tonight's deeper a11y sweep — running once the easy wins were done — surfaced four critical sub-44px / unguarded-motion sites that the swarm had not previously inspected, plus five sibling warnings. Bundle is byte-flat vs prior night, curriculum sha256 matches, engine determinism clean.

## 🔴 Critical Findings
**[C-1]** `scaffoldBanner` runs an unguarded 400ms alpha fade-in even when `prefers-reduced-motion: reduce` is set — only the `delayedCall` delay is zeroed, the inner `scene.tweens.add()` always runs — *Source: a11y-auditor A11Y-003 → `src/components/sessionComplete/scaffoldBanner.ts:81-89`* — *Why critical: violates the global reduced-motion rule and runs every session-complete flow, hitting a high-traffic surface for vestibular-sensitive users.*

**[C-2]** `ResetDeviceHandler` Reset button (BTN_H=80) and modal Yes/Cancel rectangles (240×80) all fall below the 44 CSS px floor at 360 vp (≈36 CSS px) — *Source: a11y-auditor A11Y-005 → `src/scenes/settings/ResetDeviceHandler.ts:8-9,82,96,105,119`* — *Why critical: the 3af21af Settings refactor raised parent buttons to FULL_H=100 but the reset-confirm modal was missed; touch failures on a destructive irreversible action.*

**[C-3]** `OrderInteraction` submit button hit rectangle is 200×52 (≈23 CSS px tall @360 vp), half the WCAG 2.5.5 minimum — *Source: a11y-auditor A11Y-006 → `src/scenes/interactions/OrderInteraction.ts:184-187`* — *Why critical: blocks core archetype completion for pointer users on small viewports; sibling archetypes use 100 px hit zones.*

**[C-4]** `SnapMatchInteraction` and `CompareInteraction` visual buttons present a 56 px feedback surface that may mislead users about the actual tap target (hit-zone passes at 100 px, visual does not) — *Source: a11y-auditor A11Y-007 → `src/scenes/interactions/SnapMatchInteraction.ts:179-181`, `src/scenes/interactions/CompareInteraction.ts:142-160`* — *Why critical: subagent-flagged severity; visual/hit divergence is a known accessibility anti-pattern even when hit-test passes.*

## 🟡 Warnings
**[W-1]** `QuestCompleteOverlay` retains 6 raw `scene.tweens.add()` calls instead of the project-mandated `tween()` wrapper from `scenes/utils/motion.ts` — *Source: a11y-auditor A11Y-001 → `src/components/QuestCompleteOverlay.ts:125,221,228,246,267,273`* — sibling cleanup left over from the G2 fix.

**[W-2]** `UpdateBanner` uses raw `scene.tweens.add()` instead of `tween()` helper — guarded by an explicit `if (!reduceMotion)` so behavior is correct, but bypasses the linted helper — *Source: a11y-auditor A11Y-002 → `src/components/UpdateBanner.ts:94`* — sibling cleanup left over from the G3 fix.

**[W-3]** `PostSessionOverlay` TERTIARY (h=60) and SECONDARY (h=90) buttons translate to ≈27/40.5 CSS px @360 vp — below 44 CSS px — *Source: a11y-auditor A11Y-004 → `src/components/PostSessionOverlay.ts:60`* — only PRIMARY (h=110) meets the bar.

**[W-4]** `FirstRunScene` "No thanks, just play" skip button is 260×80 (≈36 CSS px @360 vp) — A11yLayer parity exists for keyboard users but pointer users see a sub-44px surface — *Source: a11y-auditor A11Y-008 → `src/scenes/FirstRunScene.ts:228-244`*

**[W-5]** `StudentSwitcher` chip and Add Player button are both 80 canvas px (≈36 CSS px @360 vp); profile cards at 100 px are fine — *Source: a11y-auditor A11Y-009 → `src/components/StudentSwitcher.ts:39,47`*

**[W-6]** `phaser` chunk at 90.0% of its 380 KB sub-budget (342.1 KB gzipped) — zero headroom for any phaser version bump — *Source: bundle-watcher BUNDLE-003 → `dist/assets/phaser-DM3nDuma.js`* — carry-forward from 2026-05-07/08.

**[W-7]** Application manifest fails JSON parse at boot (`The manifest is not valid JSON data.`) — *Source: `test-results/nightly-screenshots/console.log:1`* — carry-forward from 2026-05-07/08.

## 🟢 Info / Observations
**[I-1]** Total gzipped JS: 518.4 KB / 1024 KB (50.6% of 1 MB budget). Brotli total: 423.1 KB. PASS with 49.4% headroom. — *bundle-watcher BUNDLE-001*

**[I-2]** Bundle delta vs 2026-05-08: **0 bytes** — exactly identical to the byte (530,796 gzipped). Tonight's five fix commits (G1–G4 + test-mock update) are bundle-neutral despite being behaviorally meaningful. — *bundle-watcher BUNDLE-002*

**[I-3]** Per-chunk health: `scenes` 82.1 KB / 100 KB (82.1%); `dexie` 30.3 KB / 35 KB (86.4%); `observability` 8.2 KB / 12 KB (68.1%, dormant by default but still shipped). All flat vs 2026-05-08. — *bundle-watcher BUNDLE-004*

**[I-4]** Top 5 chunks gzipped: `phaser` 342.1, `scenes` 82.1, `dexie` 30.2, `index` 9.3, `observability` 8.2 KB — five largest = 472 KB / 91% of total. Other 40+ chunks contribute <10 KB combined; code-splitting on archetypes/scenes/repos working as designed. — *bundle-watcher BUNDLE-005*

**[I-5]** Curriculum byte-parity holds: `public/curriculum/v1.json`, `src/curriculum/bundle.json`, and the deployed copy fetched at runtime all sha256 to `e100939946cad525edf755b832c0404c49846f6590df19c0ff0cd2cbc64f3541`. — *curriculum-byte-parity (zero findings) + `curriculum-hash.json` (match=true)*

**[I-6]** C1–C10 fully clean across HEAD~10..HEAD (G1–G4 + nightly infra commits). No new egress, no new deps, no new localStorage keys, no curriculum/level changes, no neon/particle additions. C7 actively improved by versionTapToggle (360×100 hit zone) and SettingsScene (FULL_H=100). C10 confirmed: every fix commit serves accessibility correctness for the validation playtest population. — *c1-c10-auditor C-001..C-010*

**[I-7]** Engine determinism: zero findings — no host-global RNG/clock calls introduced under `src/engine/**`. — *engine-determinism-auditor*

**[I-8]** G1–G4 spot-checks all PASS: versionTapToggle 360×100 hit zone + A11yLayer.mountAction registered; QuestCompleteOverlay buttons mounted with 100 px hit rectangles; UpdateBanner A11yLayer parity + reduce-motion guard correct; VignetteAnimations now imports `tween()` and uses it for createAnimateInTweens/createAnimateOutTweens. — *a11y-auditor A11Y-011*

**[I-9]** `src/components/sessionComplete/animations.ts` uses 6 raw `scene.tweens.add()` calls — gating delegated to caller (`SessionCompleteOverlay`), so currently safe but bypasses `tween()` wrapper. Lower priority than C-1 (`scaffoldBanner`) where the gating itself is missing. — *a11y-auditor A11Y-010*

**[I-10]** localStorage at boot is empty (`{}`) on a fresh nightly profile — no orphaned student keys, no unknown keys, matches expected pre-onboarding state. — *`localstorage.json`*

**[I-11]** Canvas sizing on iPhone-class viewport (390×844): canvas 800×1280 → CSS 390×624, no right/bottom clip. Portrait 800×1280 backing store correct. — *`canvas-sizing.json`*

**[I-12]** CSP working as intended: Cloudflare beacon (`static.cloudflareinsights.com/beacon.min.js`) refused; one inline script also refused — preserves C1 no-egress posture. — *`console.log:2-3`*

**[I-13]** 6 source-touching commits since prior nightly (`e2a4e39..HEAD`): `c2966df` G1, `38a73e4` G2, `b94071c` G3, `4edb7c2` G4, `607accf` test mock update, `87baf36` PR record chore. All four G-groups address findings W-1..W-4 from 2026-05-08.

## 🆕 New Since 2026-05-08
- **C-1** (scaffoldBanner unguarded fade-in)
- **C-2** (ResetDeviceHandler sub-44px in modal)
- **C-3** (OrderInteraction submit hit zone 52 px)
- **C-4** (SnapMatch/Compare visual/hit divergence)
- **W-1** (QuestCompleteOverlay raw tweens — sibling of resolved W-2 prior)
- **W-2** (UpdateBanner raw tween — sibling of resolved W-3 prior)
- **W-3** (PostSessionOverlay TERTIARY/SECONDARY hit zones)
- **W-4** (FirstRunScene skip button)
- **W-5** (StudentSwitcher chip)
- **I-2** (bundle delta exactly 0 bytes — first time recorded)
- **I-9** (sessionComplete/animations.ts raw tweens — info severity)

## ♻️ Unchanged from 2026-05-08
- **W-6** (phaser chunk at 90% of sub-budget) — was W-5 prior
- **W-7** (manifest JSON parse error) — was W-6 prior

The four Settings/overlay-scope warnings (W-1, W-2, W-3, W-4 of `2026-05-08.md`) are now **resolved** by merged commits `c2966df`, `38a73e4`, `b94071c`, `4edb7c2` and verified clean by tonight's a11y audit (A11Y-011).

## Swarm Audit Summary
| Subagent | Findings |
|----------|----------|
| a11y-auditor | 11 findings (4 critical, 5 warning, 2 info) |
| bundle-watcher | 5 findings (1 warning, 4 info) |
| c1-c10-auditor | 10 findings (10 info — all C1–C10 clean) |
| curriculum-byte-parity | 0 findings (clean — sha256 match) |
| engine-determinism-auditor | 0 findings (clean) |

## Commits Since Last Report
- `87baf36` chore: 2026-05-08 nightly fix PR record
- `607accf` test(UpdateBanner): mock A11yLayer.mountAction after G3 swap
- `4edb7c2` fix(a11y): VignetteAnimations uses tween() helper for reduced-motion compliance [nightly-2026-05-08 group G4]
- `b94071c` fix(a11y): UpdateBanner mounts via A11yLayer for production SR users [nightly-2026-05-08 group G3]
- `38a73e4` fix(a11y): QuestCompleteOverlay buttons mount via A11yLayer [nightly-2026-05-08 group G2]
- `c2966df` fix(a11y): version-tap-toggle touch target + A11yLayer parity [nightly-2026-05-08 group G1]

## Screenshots Captured
- `test-results/nightly-screenshots/menu-chromium.png` — Chromium MenuScene render
- `test-results/nightly-screenshots/menu-webkit.png` — WebKit MenuScene render
- `test-results/nightly-screenshots/console.log` — boot console output
- `test-results/nightly-screenshots/localstorage.json` — fresh-profile LS audit
- `test-results/nightly-screenshots/canvas-sizing.json` — viewport/canvas geometry
- `test-results/nightly-screenshots/curriculum-hash.json` — deployed vs bundle sha256

## Unstaged Source Changes at Test Time
None in `src/`. Working tree carries doc-only modifications (`.claude/agents/*.md`, `.claude/commands/*.md`, `CLAUDE.md` from `sync:claude-md` runs) and accumulated `.claude/sessions/*.md` session logs — no impact on nightly findings.

## Known baseline — never flag as new
- 4x 'Access to storage is not allowed from this context' at boot (SW context, tracked)
- 'Persistence granted: false' at boot (browser default)
- unlockedLevels:* and completedLevels:* in localStorage (known C5 deviation)
- Canvas 800x1280 portrait (correct)
- 0 native focusable DOM elements (A11y layer handles keyboard — correct)
