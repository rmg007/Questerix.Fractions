# Nightly Fix PR — 2026-05-08

## PR
- **URL:** https://github.com/rmg007/Questerix.Fractions/pull/103
- **Branch:** `fix/2026-05-08-nightly`
- **Head:** `607accfb`
- **Base:** `main`
- **Title:** `fix: nightly findings 2026-05-08 — A11y siblings + reduced-motion gaps`

## Triage outcome
- 4 AUTO-FIX (W-1, W-2, W-3, W-4) — all landed
- 2 HUMAN-REVIEW (W-5 phaser sub-budget, W-6 manifest 404) — left for human decision

## Commits on branch
| SHA | Subject |
|---|---|
| `c2966df` | fix(a11y): version-tap-toggle touch target + A11yLayer parity [G1] |
| `38a73e4` | fix(a11y): QuestCompleteOverlay buttons mount via A11yLayer [G2] |
| `b94071c` | fix(a11y): UpdateBanner mounts via A11yLayer for production SR users [G3] |
| `4edb7c2` | fix(a11y): VignetteAnimations uses tween() helper for reduced-motion compliance [G4] |
| `607accf` | test(UpdateBanner): mock A11yLayer.mountAction after G3 swap |

## Verification
| Check | Result |
|---|---|
| typecheck | ✅ clean |
| lint | ✅ 0 errors, 63 pre-existing Phase-5 warnings |
| unit | ✅ 1023 passed, 8 skipped, 0 failed |
| e2e (orchestrator nightly run, chromium-desktop) | ⚠️ pre-existing flakes — none new |

## Notes
- Round 3 of the orchestrator initially failed because the G3 swap broke the existing `UpdateBanner.test.ts` (mock targeted the old `TestHooks.mountInteractive` path). Fixed in `607accf`; full unit suite green afterward.
- Parallel-fixer attribution drift: G1's file change landed inside G2's commit due to `lint-staged` cross-contamination between concurrent agents. All changes are present and correct; only the commit subject/sha mapping is shuffled.
- Two stale worktrees (`agent-a24c9b7b...`, `agent-a3eb0c7d...`) refused to delete during pre-flight — Permission denied on Windows. Cosmetic; does not block subsequent runs.
