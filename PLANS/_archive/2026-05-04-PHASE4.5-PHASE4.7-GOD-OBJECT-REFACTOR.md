# Questerix Fractions — Active Plan (2026-05-04)

**Current Status:** Phase 17 complete. Phase 4 god-object refactoring in progress.
**Objective:** Repository cleanup, status audit, CI resolution, and Phase 4.5/4.7 execution.

---

## 1. Status Audit & Cleanup
- [ ] **Phase 4.3 & 4.4 Documentation:** Verify if extra closeout reports are needed beyond the `PLANS/PLAN.md` summary.
- [ ] **CI Failure Resolution:** Fix `claude-md-maintenance.yml` and `misconception-synthesis.yml` failures.
  - Potential fix: Add `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true` to match other working workflows.
  - Potential fix: Verify `GH_TOKEN` and `ANTHROPIC_API_KEY` usage.
- [ ] **Repository Hygiene:** Ensure all temporary files are pruned.

## 2. Phase 4.5 — SettingsScene Refactor
- **File:** `src/scenes/SettingsScene.ts` (581 LOC)
- **Goal:** Extract Reset/Data handlers to a separate module to keep the scene clean and under budget (budget is 600, currently 581, but extraction improves maintainability).
- **Extraction Targets:**
  - `ResetDeviceHandler`: The confirm/cancel UI logic and `db.delete()` execution.
  - `BackupRestoreHandler`: `doExport` and `doRestore` logic.

## 3. Phase 4.7 — SessionCompleteOverlay Refactor
- **File:** `src/components/SessionCompleteOverlay.ts` (280 LOC)
- **Goal:** Even though it's under 300, it's a "god component". Extract animation logic and scoring math.
- **Extraction Targets:**
  - `SessionScoreCalculator`: Math for mastery and session outcomes.
  - `SessionOverlayAnimations`: Tween logic for the overlay appearance.

## 4. GitHub Workflow Follow-up
- [ ] Verify CI passes after fixes.
- [ ] Push changes to `main`.

---

## Session Log (2026-05-04)

- **Starting Audit:** Verified `LevelScene.ts` (591), `SettingsScene.ts` (581), `MenuScene.ts` (432), and `Mascot.ts` (256) are all currently under budget.
- **Workflow Investigation:** Found that `ci.yml`, `deploy.yml`, and `synthetic-playtest.yml` all use `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true`, but the failing workflows do not.
