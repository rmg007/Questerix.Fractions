# Phase 4.1 (Level01Scene refactor) + UI/UX Polish — Closeout

**Date:** 2026-05-02
**Branch:** main

---

## Phase 4.1 — Level01Scene God-Object Refactor ✅

**Goal:** Reduce `src/scenes/Level01Scene.ts` from ~1648 LOC → ≤ 600 LOC.

**Result:** 1648 → 598 LOC (**64% reduction**).

### Gate criteria — all passing
- ✅ typecheck clean (`npm run typecheck`)
- ✅ lint clean (only unrelated `LevelVignette.ts` exceeds limit)
- ✅ 702 unit tests pass
- ✅ file under 600 LOC budget

### Extracted modules (10 new files)
| File | Purpose |
|---|---|
| `src/scenes/Level01SceneLayout.ts` | UI chrome creation (header, prompt, hint, submit buttons) |
| `src/scenes/Level01SceneShapeRenderer.ts` | Shape rendering, partition line, ghost guide, correct feedback |
| `src/scenes/Level01SceneHintSystem.ts` | Hint tier escalation, display, recording |
| `src/scenes/Level01SceneSelection.ts` | BKT-adaptive question selection |
| `src/scenes/Level01SceneValidation.ts` | Answer validation flow |
| `src/scenes/Level01SceneStreakBanner.ts` | Streak milestone banner animation |
| `src/scenes/Level01SceneOutcomeFlow.ts` | Feedback announcements + microcopy |
| `src/scenes/Level01SceneSetup.ts` | A11y, test hooks, prefs setup |
| `src/scenes/Level01SceneDragHandle.ts` | Drag handle factory |
| `src/lib/level01SessionComplete.ts` | Session completion flow |

---

## UI/UX Polish — 14 Items Complete ✅

### Substantive changes (3)
- **UI-S1** Partition line: 16px navy + 2px white outline (was 10px solid)
  → `src/scenes/Level01SceneShapeRenderer.ts:84-93`
- **UI-1** Drag handle gripper: circular r=20, white border, ‹ › chevrons, 2-pulse on load
  → `src/components/DragHandle.ts`
- **UI-S2** Hint button text: "💡 Need a hint?" (was "💡 Hint")
  → `src/scenes/utils/levelTheme.ts:213`

### Already implemented — verified in place (9)
- **T25.A** Splash adventure background + fraction tiles → `index.html` + `PreloadScene.ts`
- **T25.B** Camera fades on all transitions → `fadeAndStart` used universally
- **T25.C** Star progress strip → `ProgressBar.ts` (5 ☆/★ with bounce)
- **T26.A** Sparkle burst on correct → `FeedbackAnimations.ts` `burstStarParticles`
- **T26.B** Trophy completion screen → `SessionCompleteOverlay.ts`
- **T27** Mascot states (idle/think/cheer/cheer-big/oops/celebrate/wave/sleep) → `Mascot.ts`
- **T19** World map (9 winding-path nodes) → `LevelMapScene.ts`
- **T18** Theme polish tokens (header pills, prompt panel, hint amber, TTS pill) → `levelTheme.ts`
- **S** Layout collapse → already applied in extracted `Level01SceneLayout.ts`

### Skipped — design conflicts (2)
- **UI-M1** MenuScene Play! button at y=760–840 — current design intentionally places Play at the bottom (y=1100) of the wavy 0 → ½ → 1 number-line metaphor. Moving it would break the pedagogical composition.
- **UI-M2** MenuScene Quest reposition — same number-line layout reasoning.

---

## Verification

```bash
npm run typecheck    # ✅ clean
npm run lint         # ✅ clean (LevelVignette.ts is unrelated, pre-existing)
npm run test:unit    # ✅ 702 tests pass
wc -l src/scenes/Level01Scene.ts  # 598 (under 600 budget)
```
